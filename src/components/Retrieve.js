import React from 'react'

export default function Retrieve(props) {

    function display(json) {
        json.map(json=>{
            return (
                <div>

                </div>
            )
        })